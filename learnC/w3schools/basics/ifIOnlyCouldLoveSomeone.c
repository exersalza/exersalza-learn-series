#include <stdio.h>

void main() {
	// Comparison operators
	// < - less than
	// > - greater than
	// <= - less than or equal to
	// >= - greater than or equal to
	// == - equal to
	// != - not equal to
	//
	// && - and
	// || - or
	// ! - not

	// Conditional statements
	// if (condition) {}
	// else if (condition) {}
	// else {}
	// switch (expression) {
	// 	case value:
	// 		break;
	// 	default:
	// 		break;
	// }

	if (1 == 1) { // should be true right?
		printf("1 is equal to 1\n");
	} else {
		printf("1 is not equal to 1\n");
	}
}
